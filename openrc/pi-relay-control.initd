#!/sbin/openrc-run

name="pi-relay-control"
description="GPIO relay control daemon"

command="/usr/bin/relay_control"
command_user="root"

supervisor=supervise-daemon
respawn_delay=5
respawn_max=0

pidfile="/run/${RC_SVCNAME}.pid"
output_log="/var/log/${RC_SVCNAME}.log"
error_log="/var/log/${RC_SVCNAME}.log"

depend() {
    need net
    after firewall
}

start_pre() {
    if [ ! -f /.successfully-initialized ]; then
        eerror "pi-relay-control: /.successfully-initialized not found -- device not yet provisioned, refusing to start"
        return 1
    fi
    checkpath -d -m 0755 -o root:root /var/lib/relay_control
}
